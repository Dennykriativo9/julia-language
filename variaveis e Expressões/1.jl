#1. Faça um programa que leia um numero inteiro e o imprima.

num = readline()  # o readline lê qualquer formato e deixa na forma de string
num_int = parse(Int64,num) # Precisamos do Parse para fazer a Conversão 
print(num_int)