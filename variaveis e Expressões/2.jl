# 2. Faça um programa que leia um numero real e o imprima.

num = readline()  # o readline lê qualquer formato e deixa na forma de string
num_float = parse(Float64,num) # Precisamos do Parse para fazer a Conversão 
print(num_float)